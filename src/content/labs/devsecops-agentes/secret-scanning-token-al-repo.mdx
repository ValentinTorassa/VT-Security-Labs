---
title: "Secret scanning: el token que se te escapó al repo"
description: "Tarde o temprano alguien commitea una API key. Aprendé a detectarla con escaneo automático, a frenarla antes del commit y qué hacer cuando ya se filtró."
path: "devsecops-agentes"
order: 2
level: "beginner"
estimatedMinutes: 30
prerequisites:
  - "Saber hacer un commit con git"
  - "Una terminal en Linux, macOS o WSL"
objectives:
  - "Entender por qué borrar el secreto en un commit nuevo no alcanza"
  - "Correr un escaneo de secretos sobre un repo"
  - "Frenar un secreto antes del commit con un hook"
  - "Saber el orden correcto de respuesta cuando una credencial se filtra"
evidence:
  - "Salida (sintética) de un escaneo que encuentra una key"
  - "Tu config de pre-commit que bloquea el commit"
  - "Checklist de rotación: los pasos que harías ante una fuga real"
commands:
  - "gitleaks detect --source ."
  - "git log -p -S 'AKIA'"
challenge: "Creá un repo de juguete, commiteá una key FALSA tipo AKIAEXAMPLE..., corré un escaneo que la detecte y después escribí el checklist de qué harías si fuera real. La clave: rotar primero."
nextLabs:
  - "cloud-produccion/iam-deny-gana"
  - "ciberseguridad/docker-socket-to-host"
tags:
  - "devsecops"
  - "secretos"
  - "git"
---

import Callout from "../../../components/Callout.astro";
import TerminalBlock from "../../../components/TerminalBlock.astro";
import Quiz from "../../../components/Quiz.astro";

Le pasa a todos: una API key hardcodeada "para probar", un `.env` que se coló, un token en un script de deploy. Lo commiteás, lo pusheás, y ya está afuera. El problema no es solo que esté en el archivo de hoy: es que **git tiene memoria**.

<Callout type="concepto">
  Un secreto que entró a la historia de git no se va borrándolo en un commit
  nuevo. El commit viejo sigue ahí, con la key adentro, y cualquiera que clone
  el repo la tiene con un `git log`. En un repo público, asumí que un bot la
  encontró **en minutos**.
</Callout>

## Por qué `.gitignore` no te salva después

`.gitignore` evita que un archivo **entre** al repo. No saca nada de lo que ya entró. Si commiteaste `.env` y después lo agregás al `.gitignore`, el archivo deja de trackearse de ahí en más, pero el commit donde lo subiste sigue conteniéndolo.

<TerminalBlock title="vt@labs:~">

```bash
# ¿Quedó alguna key de AWS en TODA la historia, no solo en el último commit?
git log -p -S "AKIA" -- . | grep -i "AKIA"
```

</TerminalBlock>

`-S "AKIA"` busca commits donde aparezca o desaparezca ese texto. Es la diferencia entre mirar el presente y auditar el pasado.

## Detección automática: un escáner

Revisar a mano no escala. Una herramienta de **secret scanning** (como gitleaks) recorre todo el repo y su historia buscando patrones de credenciales: keys de AWS, tokens, claves privadas.

<TerminalBlock title="vt@labs:~">

```bash
gitleaks detect --source . --verbose
```

</TerminalBlock>

Una detección se ve, más o menos, así (datos **sintéticos**):

```text
Finding:     AKIAEXAMPLE0000000000
Secret:      AKIAEXAMPLE0000000000
RuleID:      aws-access-key-id
File:        deploy/setup.sh
Commit:      3f9a1c2  (hace 4 días)
Author:      deploy-bot <bot@example.com>
```

<Callout type="comando">
  Lo potente del escaneo es que mira **la historia completa**, no el working
  tree. Te dice en qué commit entró el secreto y quién lo subió — justo lo que
  necesitás para responder.
</Callout>

## Frenarlo antes de que pase: pre-commit hook

Mejor que detectarlo después es no dejarlo entrar. Un hook de pre-commit corre el escáner **antes** de cada commit y lo aborta si encuentra algo.

```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.0
    hooks:
      - id: gitleaks
```

```text
$ git commit -m "agrego deploy"
gitleaks……………… Failed
  ✗ AKIAEXAMPLE0000000000  en deploy/setup.sh
commit abortado: sacá el secreto antes de continuar
```

<Callout type="concepto">
  Defensa en capas: el **hook** te frena en tu máquina (rápido, pero se puede
  saltear con `--no-verify`), y el **escaneo en CI** te frena en el server (no
  se saltea, es la red de contención real). Por eso conviene tener los dos: uno
  te avisa temprano, el otro no perdona.
</Callout>

## Ya se filtró: el orden importa

Si un secreto real llegó a un repo (sobre todo público), hay un orden que no se negocia:

```text
1. ROTAR primero.   Generá una credencial nueva y revocá la vieja YA.
                    Mientras la vieja sirva, el repo limpio no importa.
2. Recién después,  limpiá la historia (o, muchas veces, archivá el repo
                    y arrancá uno limpio: reescribir historia es traicionero).
3. Investigá.       ¿Se usó esa key? Mirá los logs (acá entra CloudTrail).
4. Prevení.         Sumá el hook + el escaneo en CI para que no se repita.
```

<Callout type="error">
  El error clásico es invertir el orden: borrar el secreto del repo primero y
  rotarlo "después, con calma". Mientras esa credencial siga siendo válida, da
  igual lo prolijo que quede el repo — la puerta sigue abierta. **Rotás primero,
  limpiás después.**
</Callout>

<Callout type="evidencia">
  Para este lab usá siempre keys **ficticias** (`AKIAEXAMPLE...`). El objetivo
  es practicar la detección y la respuesta, no pasear un secreto real por tu
  terminal ni por un repo.
</Callout>

<Quiz
  title="Comprobá lo que entendiste"
  questions={[
    {
      q: "Commiteaste una API key, la borraste en el commit siguiente y agregaste el archivo al .gitignore. ¿Está resuelto?",
      options: [
        "Sí, el archivo ya no está y .gitignore evita que vuelva",
        "No: el commit viejo sigue en la historia con la key adentro",
        "Sí, siempre que hayas hecho git push --force",
      ],
      answer: 1,
      explain:
        "El secreto vive en el commit donde entró. Borrarlo después no lo saca de la historia, y .gitignore solo aplica de ahí en más. Hay que rotar la credencial.",
    },
    {
      q: "Una API key real se filtró a un repo público. ¿Cuál es el primer paso?",
      options: [
        "Reescribir la historia de git para borrarla",
        "Rotar la credencial: emitir una nueva y revocar la vieja",
        "Hacer el repo privado y seguir como si nada",
      ],
      answer: 1,
      explain:
        "Rotar primero. Mientras la credencial vieja sea válida, qué tan limpio quede el repo no cambia que la puerta sigue abierta.",
    },
    {
      q: "¿Por qué tener escaneo en CI si ya hay un hook de pre-commit?",
      options: [
        "Porque el hook local se puede saltear (--no-verify); el CI no",
        "Porque el CI es más rápido que el hook",
        "Porque el hook no detecta keys de AWS",
      ],
      answer: 0,
      explain:
        "El hook es una ayuda temprana pero opcional: cualquiera lo evita con --no-verify. El escaneo en CI corre en el server y es la barrera que no se saltea.",
    },
  ]}
/>

Los secretos se van a filtrar; es cuestión de cuándo. Lo que define si es un susto o un incidente es tener la detección puesta **y** saber que ante una fuga se rota primero y se limpia después.
